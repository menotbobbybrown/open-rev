{
  "name": "@openrev/desktop",
  "version": "0.1.0-alpha.2",
  "private": true,
  "description": "OpenRev Tauri desktop host — real Rust command bridge (file dialog, analysis sidecar, error reporting); requires the Rust toolchain to build",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.0"
  },
  "license": "Apache-2.0"
}
